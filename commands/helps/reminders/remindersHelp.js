import { configDotenv } from "dotenv"
export default {
    name: "help remind",
    type: 'helps',
    usage: `{process.env.prefix}remind <query> task name - <time_format>`,
    execute : async () =>{
        
    }
}